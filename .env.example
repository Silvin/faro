# Copia a .env y ajusta. NUNCA subas .env al repo.

# Backend
PORT=8080
DATABASE_URL=postgres://faro:faro@localhost:5432/faro?sslmode=disable
JWT_SECRET=cambia-esto-en-produccion
CORS_ORIGIN=http://localhost:3000
COOKIE_SECURE=false   # true en producción (HTTPS)

# Seed del super admin global (tarea T2)
FARO_SUPERADMIN_EMAIL=admin@faro.app
FARO_SUPERADMIN_PASSWORD=cambia-esto

# Tests de integración (DB migrada y vacía aparte)
# TEST_DATABASE_URL=postgres://faro:faro@localhost:5432/faro_test?sslmode=disable

# Frontend (próximo incremento)
NEXT_PUBLIC_API_URL=http://localhost:8080
